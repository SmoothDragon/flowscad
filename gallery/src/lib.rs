//! `flowscad-gallery` — compile-checked collection of personal `flowscad` models.
//!
//! This crate has no library API. It exists so the many one-off design files under
//! `gallery/examples/` continue to build (and can be run with
//! `cargo run --example <name>`), without slowing down the main `flowscad` crate's
//! `cargo build --examples`.
